import { Canvas, Meta, Controls, Source } from '@storybook/blocks';
import { Fragment } from 'react';

import { transforms, transformStrings } from './qti-transform.examples';

<Meta title="QTI Transform" />

# qti-transform

<table>
  <thead>
    <tr>
      <th>Input</th>
      <th>Output</th>
    </tr>
  </thead>
  <tbody>
    {Object.keys(transforms).map((transform, index) => (
<Fragment key={index}>
    <tr>
            <td colSpan={2}>
          <pre>{transform}</pre>
        </td>

</tr>
      <tr >

        <td>
          <pre><small>{transformStrings[transform + 'String']}</small></pre>
        </td>
        <td>
          <pre><small>{transforms[transform]}</small></pre>
        </td>
      </tr>
      </Fragment>
    ))}

  </tbody>
</table>
